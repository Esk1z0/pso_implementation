class ExperimentRunner:
    def run_experiment(self, config)
    def run_multiple_trials(self, n_trials, config)
    def compare_parameters(self, param_grid)
    def save_results(self, results, filename)